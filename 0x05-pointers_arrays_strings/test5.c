#include <stdio.h>

void rev_string(char *s);
/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    char s[10] = "My School";

    printf("%s\n", s);
    rev_string(s);
    printf("%s\n", s);
    return (0);
}

void rev_string(char *s)
{
	int a, b, c;

	for (a = 0;*(a + s) != '\0'; a++)
	{
		c=a;
	}
	putchar('\n');
	for (b = c; b >= 0; b--)
		for (d = 0; d !
		s[b-c]=
		putchar(s[b]);
	putchar('\n');
}
